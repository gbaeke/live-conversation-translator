{
  "name": "live-conversation-translator",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "node --check api/session.js && node --check src/main.js"
  },
  "devDependencies": {
    "vite": "^7.1.2"
  }
}
